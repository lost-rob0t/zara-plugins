name: Nix Bash Clarification Continuity E2E

on:
  pull_request:
    paths:
      - 'plugins/zara-nix-expert/**'
      - 'plugins/zara-bash-expert/**'
      - 'plugins/zara-expert/**'
      - 'tests/test_nix_bash_clarification_continuity_e2e.py'
      - 'tests/test_nix_bash_durable_persistence_e2e.py'
      - '.github/workflows/nix-bash-clarification-continuity-e2e.yml'

permissions:
  contents: read

concurrency:
  group: nix-bash-clarification-continuity-${{ github.ref }}
  cancel-in-progress: true

jobs:
  clarification-continuity:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Fetch exact current Dotfiles Nix Bash brains
        shell: bash
        run: |
          set -euo pipefail
          revision=3309c54ecb5f65c29374de6c60d2135a9ea2f94b
          rm -rf .ci/dotfiles-nix-bash-clarification
          git init .ci/dotfiles-nix-bash-clarification
          git -C .ci/dotfiles-nix-bash-clarification remote add origin https://github.com/lost-rob0t/dotfiles.git
          git -C .ci/dotfiles-nix-bash-clarification config core.sparseCheckout true
          mkdir -p .ci/dotfiles-nix-bash-clarification/.git/info
          printf '%s\n' \
            '/.zara/experts/nix/' \
            '/.zara/experts/bash/' \
            '/.zara/experts/language/' \
            > .ci/dotfiles-nix-bash-clarification/.git/info/sparse-checkout
          git -C .ci/dotfiles-nix-bash-clarification fetch --depth=1 origin "$revision"
          git -C .ci/dotfiles-nix-bash-clarification checkout --detach FETCH_HEAD
          test "$(git -C .ci/dotfiles-nix-bash-clarification rev-parse HEAD)" = "$revision"

      - name: Fetch exact current Zara Core conversation authority
        shell: bash
        run: |
          set -euo pipefail
          revision=00b3d7ce39f925b504f9094908e97ff42ae7a169
          rm -rf .ci/zara-core-nix-bash-clarification
          git init .ci/zara-core-nix-bash-clarification
          git -C .ci/zara-core-nix-bash-clarification remote add origin https://github.com/lost-rob0t/zara.git
          git -C .ci/zara-core-nix-bash-clarification fetch --depth=1 origin "$revision"
          git -C .ci/zara-core-nix-bash-clarification checkout --detach FETCH_HEAD
          test "$(git -C .ci/zara-core-nix-bash-clarification rev-parse HEAD)" = "$revision"

      - name: Install symbolic runtime and network audit tool
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends swi-prolog-nox strace
          python3 -m pip install \
            'numpy>=1.26' \
            'soundfile>=0.12' \
            'pyswip>=0.3.1' \
            'pykka>=3.1' \
            'langchain-core>=0.2'

      - name: Prove Nix Bash evidence survives clarification and restart with zero providers
        env:
          ZARA_DOTFILES_ROOT: ${{ github.workspace }}/.ci/dotfiles-nix-bash-clarification
          ZARA_CURRENT_CORE_ROOT: ${{ github.workspace }}/.ci/zara-core-nix-bash-clarification
        shell: bash
        run: |
          set -euo pipefail
          trace="$RUNNER_TEMP/nix-bash-clarification-continuity-network.trace"
          env \
            -u OPENAI_API_KEY \
            -u ANTHROPIC_API_KEY \
            -u OPENROUTER_API_KEY \
            -u GOOGLE_API_KEY \
            -u GEMINI_API_KEY \
            -u GROQ_API_KEY \
            -u ZAI_API_KEY \
            -u AZURE_OPENAI_API_KEY \
            -u HF_TOKEN \
            strace -f -e trace=network -o "$trace" \
            python3 -m unittest discover -s tests -t . -p 'test_nix_bash_clarification_continuity_e2e.py'
          if grep -Eq 'socket\(AF_INET6?,' "$trace"; then
            echo 'unexpected IPv4/IPv6 network syscall during Nix/Bash clarification continuity E2E' >&2
            cat "$trace" >&2
            exit 1
          fi
